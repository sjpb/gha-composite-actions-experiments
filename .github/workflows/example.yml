name: Demo using shared setup action
on:
  push:
jobs:
  demo:
    name: demo-job
    runs-on: ubuntu-22.04
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: list all
        run: ls -al
      - name: run setup
        uses:  ./.github/actions/setup
      - name: Show openstack client is installed
        run: |
          . venv/bin/activate
          openstack --help
